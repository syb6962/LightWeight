package com.example.lightweight.data.db

import android.app.Application

class DetailApplication : Application() {
//    val database by lazy { WorkoutDatabase.getDatabase(this) }
//    val repository by lazy { DetailRepository(database.detailDao()) }
}